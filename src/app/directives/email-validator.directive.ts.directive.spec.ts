import { EmailValidatorDirectiveTsDirective } from './email-validator.directive.ts.directive';

describe('EmailValidatorDirectiveTsDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailValidatorDirectiveTsDirective();
    expect(directive).toBeTruthy();
  });
});
